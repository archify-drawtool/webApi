name: Acceptatie CI

on:
  push:
    branches: [ Acceptatie ]
  pull_request:
    branches: [ Acceptatie ]

jobs:
  laravel-checks:
    runs-on: ubuntu-latest
    environment: acceptatie

    steps:
      - uses: actions/checkout@v5

      - name: Setup PHP 8.4
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, pdo, pdo_mysql

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress

      - name: Copy .env
        run: cp .env.example .env

      - name: Generate app key
        run: php artisan key:generate

# add when we have a database

#      - name: Configure database
#        run: |
#          echo "DB_CONNECTION=mysql" >> .env
#          echo "DB_HOST=${{ secrets.DB_HOST }}" >> .env
#          echo "DB_PORT=${{ secrets.DB_PORT }}" >> .env
#          echo "DB_DATABASE=${{ secrets.DB_DATABASE }}" >> .env
#          echo "DB_USERNAME=${{ secrets.DB_USERNAME }}" >> .env
#          echo "DB_PASSWORD=${{ secrets.DB_PASSWORD }}" >> .env
#
#      - name: Run migrations
#        run: php artisan migrate --force

      - name: Run linting (Pint)
        run: ./vendor/bin/pint --test

      - name: Run tests
        run: php artisan test

  docker-build-and-push:
    needs: laravel-checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build Docker image
        run: docker build -t "${{ secrets.DOCKER_USERNAME }}/archify-api:acceptatie" .

      - name: Push to Docker Hub
        run: docker push "${{ secrets.DOCKER_USERNAME }}/archify-api:acceptatie"
